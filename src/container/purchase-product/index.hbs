{{! 
Це код container файлу для index сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
 }}
<main class="main">
    <div class="product_content">
        <img src="/img/image616.jpg" alt="">
        <div>
            {{#with product}}
            <h1>{{name}}</h1>
            {{> divider}}
            <p>{{description}}</p>
            {{> divider}}
            <form class="product_form" method="POST" action="/purchase-product">
                <p>Кількість товару</p>
                <input type="number" name="amount" placeholder="Укажите количество товара">
                <button class="btn_card">
                    <a >Купить сейчас:<span> {{price}}$</span></a>
                </button>
                <input type="hidden" name="id" value="{{id}}">
            </form>
            {{/with}}
        </div>
    </div>
    <div class="other_content">
        <div class="other_content_title">
            <h1>Похожие товары</h1>
            <p>Компьютеры и ноутбуки</p>
        </div>
        <div class="other_content_cards">
            {{#each otherProducts}}
                {{> card}}
            {{/each}}
        </div>
    </div>
</main>


{{! 
    Цей код містить лише те, що буде виводитись в <body>
    Адже наш layout/default.hbs має вже структуру HTML
}}